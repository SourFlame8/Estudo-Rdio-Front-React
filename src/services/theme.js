import { extendTheme } from '@chakra-ui/react';
import "@fontsource/montserrat";

export default theme = extendTheme({
    fonts: {
        body: "Montserrat",
        heading: "Montserrat",
        mono: "Montserrat"
      }
})